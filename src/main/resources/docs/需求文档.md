# 项目需求

## v1
在html页面上有个对话，用户输入内容，通过调用大模型，返回对应信息。

大模型：使用 openAI规范调用。

## v2
给对话增加，记忆能力。

```java
    msg = input()
    memory = new list()
    memory.add(msg)
    result = openAi(msg)
    memory.add(result)
```