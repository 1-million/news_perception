# 项目需求

## v1
在html页面上有个对话，用户输入内容，通过调用大模型，返回对应信息。

大模型：使用 openAI规范调用。

## v2
给对话增加，记忆能力。

```
    msg = input()
    memory = new list()
    memory.add(msg)
    result = openAi(msg)
    memory.add(result)
```

## v3
增加 搜索实时信息的能力。
结合 可编程搜索引擎实现。

```
    webSearchEngine = webSearchEngine();
    result = webSearchEngine.search("test");
    print(result)
    // 基于搜索到的数据，今天推理总结，返回。
    // 解决模型数据过时的问题。
    response = chatModel.chat(result)
    print("response")
```

## v4
增加工具调用。
```
    // 精确描述工具。
    // 放入对话请求体。
    // LLM 根据输入内容调用对于工具。
        // 实际是返回对于工具名称和参数信息。
    // 根据 LLM 返回结果查询是否调用了工具。
    // 解析工具信息字段，使用JAVA调用对于工具。
    // 将工具执行结果，包装到工具执行结果对象。
    // 再将输入信息和工具执行结果，发送给LLM，继续推理。
```